#!/bin/zsh

set -eu
BASE_DIR=$(dirname $(cd $(dirname $0); pwd))

if [[ "$OSTYPE" == "darwin"* ]]; then
  ${BASE_DIR}/bin/setup-os-darwin
elif [[ "$OSTYPE" == "linux-gnu" ]]; then
  ${BASE_DIR}/bin/setup-os-linux
else
  echo 'Not compatible'
  exit 1
fi

